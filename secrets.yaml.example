# Copy to secrets.yaml. Credentials are not compiled in: the API key, InfluxDB token, OTA
# password and file server password are provisioned at runtime (CLAUDE.md, Credential provisioning)
influx_host: "influx.example.org"
influx_port: "443"
influx_org: "example-org"
influx_bucket: "raw"
